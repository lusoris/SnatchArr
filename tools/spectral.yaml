extends: ["spectral:oas"]
rules:
  operation-operationId: error
  operation-description: off # summaries document each operation; descriptions are optional
  info-contact: off
  operation-tags: error
  oas3-unused-component: warn
  no-$ref-siblings: off
