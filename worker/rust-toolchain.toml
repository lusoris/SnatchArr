[toolchain]
channel = "stable"
components = ["clippy", "rustfmt"]
# musl targets for the static images (worker/Dockerfile cross-links them with zig).
targets = ["x86_64-unknown-linux-musl", "aarch64-unknown-linux-musl"]
